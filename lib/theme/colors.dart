import 'package:flutter/material.dart';

const Color kTeal = Color(0xFF22E0C8);
const Color kTealDark = Color(0xFF042620);
const Color kBg = Color(0xff07070a);
const Color kBlue = Color(0xFF3A7BFF);
const Color kBgCard = Color(0xFF0A0A0C);

Color get kTealDim => kTeal.withValues(alpha: 0.12);
Color get kWhite8 => Colors.white.withValues(alpha: 0.88);
Color get kWhite5 => Colors.white.withValues(alpha: 0.05);
Color get kWhite60 => Colors.white.withValues(alpha: 0.60);
Color get kWhite45 => Colors.white.withValues(alpha: 0.45);
Color get kWhite30 => Colors.white.withValues(alpha: 0.30);
